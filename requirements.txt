numpy>=1.24
opencv-python>=4.8
librosa>=0.10
soundfile>=0.12
PyYAML>=6.0

# Optionnel — voix (prox chat) + sous-titres :
# faster-whisper>=1.0

# Optionnel — détection de l'écran de victoire par OCR :
# pytesseract>=0.3.10
